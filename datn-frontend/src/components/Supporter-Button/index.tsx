// Old livechat replaced with new chat system
// Import ChatWidget from features/chat instead
import { ChatWidget } from '../../features/chat'

const SupporterBtn = () => {
  return <ChatWidget />
}

export default SupporterBtn
